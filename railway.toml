[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm build && pnpm db:push"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/trpc/system.health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
